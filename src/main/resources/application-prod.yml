# 生产环境配置文件
spring:
  # todo 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_db
    username: root
    password: 123456
  # todo Redis 配置
  redis:
    host: localhost
    port: 6379
    database: 0

# todo 对象存储
cos:
  client:
    accessKey: xxx
    secretKey: xxx
    region: xxx
    bucket: xxx

# knife4j的增强配置
knife4j:
  enable: true
  production: true
